Practical 01 - Searching Algorithms

BFS - 
from collections import deque

def bfs(graph,start):
    visited = set()
    queue = deque([start])
   
    while queue:
        node = queue.popleft()
       
        if node not in visited:
            print(node,end=" ")
            visited.add(node)
           
            for neighbor in graph[node]:
                if neighbor not in visited:
                    queue.append(neighbor)


graph = {
    "A" : ["B","C"],
    "B" : ["D","E"],
    "C" : ["F"],
    "D" : [],
    "E" : [],
    "F" : [],
}
print("BFS GRAPH : ")
bfs(graph,"A")

DFS - 
def dfs(graph,start,visited =set()):
    if start not in visited:
        print(start,end=" ")
        visited.add(start)
       
        for neighbor in graph[start]:
            dfs(graph,neighbor,visited)


graph = {
    "A" : ["B","C"],
    "B" : ["D","E"],
    "C" : ["F"],
    "D" : [],
    "E" : [],
    "F" : [],
}


print("DFS GRAPH : ")
dfs(graph,"A")

A-Star

import heapq
def a_star(graph, start, goal, heuristic):
    open_list = []
    heapq.heappush(open_list, (0, start))


    g_cost = {start: 0}
    visited = set()

    while open_list:
        current = heapq.heappop(open_list)[1]

        if current == goal:
            print("Goal reached!")
            return

        visited.add(current)


        for neighbor, cost in graph[current]:
            new_cost = g_cost[current] + cost


            if neighbor not in g_cost or new_cost < g_cost[neighbor]:
                g_cost[neighbor] = new_cost
                f_cost = new_cost + heuristic[neighbor]
                heapq.heappush(open_list, (f_cost, neighbor))


graph = {
    'A': [('B', 1), ('C', 3)],
    'B': [('D', 1)],
    'C': [('D', 1)],
    'D': []
}


heuristic = {
    'A': 3,
    'B': 1,
    'C': 1,
    'D': 0
}

a_star(graph, 'A', 'D', heuristic)


Practical 02 - Rule based system

print("Medical System for Diagnosis")


fever = input("DO you have fever ? (yes/no)")
cough = input("DO you have cough ? (yes/no)")
rash = input("DO you have rash ? (yes/no)")
headache = input("DO you have headache ? (yes/no)")
vommiting = input("DO you have vomitting ? (yes/no)")


if fever == "yes" and cough == "yes":
    print("I thinnk you have the FLU")
elif fever == "yes" and rash == "yes":
    print("I think you have measles")
elif headache == "yes" and vommiting == "yes":
    print("You have migraine")
else:
    print("Diagnosis undetected !")

Practical 03 - Linear and Logistic Regression

Linear - 
import numpy as np
import matplotlib.pyplot as plt
from sklearn.linear_model import LinearRegression


X = np.array([[1],[2],[3],[4],[5]])
y = np.array([15000,25000,35000,45000,55000])


model = LinearRegression()
model.fit(X,y)


predict = model.predict([[7]])
print(f"Prediction for 7 is {predict}")


plt.scatter(X,y)
plt.plot(X,model.predict(X))
plt.xlabel("Experience")
plt.ylabel("Salary")
plt.show()





Logistic 
import numpy as np
import matplotlib.pyplot as plt
from sklearn.linear_model import LogisticRegression


X = np.array([[1],[2],[3],[4],[5]])
y = np.array([0,1,0,1,0])


model = LogisticRegression()
model.fit(X,y)


predict = model.predict([[6]])
print(f"PRedicted REsult is {predict}")


plt.scatter(X,y)
plt.plot(X,model.predict(X))
plt.xlabel("Nums")
plt.ylabel("PAss / Fail")
plt.show()

Practical 04 - Kmeans Clustering

from sklearn.cluster import KMeans
import matplotlib.pyplot as plt

X = [[23,25],[46,40],[53,59],[60,67],[25,30]]

kmeans = KMeans(n_clusters=3)
kmeans.fit(X)

print(kmeans.labels_)

plt.scatter([x[0] for x in X], [x[1] for x in X],c=kmeans.labels_)
plt.show()




Practical 05 - SVM and Decision Tree

SVM -
from sklearn import svm


X = [[150,7],[170,8],[130,6],[140,6.5]]
Y = ["Apples","Apples","Oranges","Oranges"]


model = svm.SVC(kernel="linear")
model.fit(X,Y)


print(model.predict([[135,6]]))

Decision Tree -
from sklearn.tree import DecisionTreeClassifier

X = [[150,7],[170,8],[130,6],[140,6.5]]
Y = ["Apples","Apples","Oranges","Oranges"]

model = DecisionTreeClassifier()
model.fit(X,Y)

print(model.predict([[135,6]]))


Practical 06 - Handwriting Analysis using Keras

Pip install -u tenserflow

from tensorflow.keras.datasets import mnist
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense, Flatten


(X_train, y_train), (X_test, y_test) = mnist.load_data()


# Normalize data
X_train = X_train / 255.0
X_test = X_test / 255.0


model = Sequential([
    Flatten(input_shape=(28,28)),
    Dense(128, activation="relu"),
    Dense(10, activation="softmax")
])


model.compile(
    optimizer="adam",
    loss="sparse_categorical_crossentropy",
    metrics=["accuracy"])


model.fit(X_train, y_train, epochs=5)
model.evaluate(X_test, y_test)




Practical 07 - Sentiment Analysis

Pip install -U textblob

from textblob import TextBlob
text = "This mobile phone is excellent and very easy to use."
analysis = TextBlob(text)

print("Sentiment Polarity:", analysis.sentiment.polarity)

if analysis.sentiment.polarity > 0:
    print("Positive Sentiment")
elif analysis.sentiment.polarity < 0:
    print("Negative Sentiment")
else:
    print("Neutral Sentiment")


Practical 08 - Performance of Classification (recall,precision,accuracy,confusion) tp,np,fp,np

from sklearn.metrics import (
    confusion_matrix,
    recall_score,
    precision_score,
    accuracy_score,
)

y_true = [1, 0, 1, 1, 1, 0, 1, 1, 0]
y_pred = [1, 0, 1, 1, 1, 0, 1, 1, 0]

print(f"Confussion matrix {confusion_matrix(y_true, y_pred)}")
print(f"Recall Score {recall_score(y_true, y_pred)}")
print(f"Percision Score {precision_score(y_true, y_pred)}")
print(f"Accuracy Score {accuracy_score(y_true, y_pred)}")


Practical 09 - Cross Validation and Hyper parameter Tuning

from sklearn.datasets import load_iris
from sklearn.neighbors import KNeighborsClassifier
from sklearn.model_selection import GridSearchCV

data = load_iris()

X = data.data
Y = data.target

model = KNeighborsClassifier()

param_grid = {"n_neighbors": [3,5,7,9]}
grid = GridSearchCV(model,param_grid,cv=5)
grid.fit(X,Y)

print(f"Best Parameter : {grid.best_params_}")
print(f"Best Score : {grid.best_score_}")



Practical 10 -Prediction

pip install -U scikit-learn
pip install pandas

from sklearn.linear_model import LinearRegression
import pandas as pd
data = {
   
    "Area" : [1000,1500,1800,2400],
    "Bedroom" :[2,3,3,4],
    "Bathroom" :[2,2,3,4],
    "Price" : [350000,380000,400000,450000]
}

df = pd.DataFrame(data)

x = df[["Area","Bedroom","Bathroom"]]
y = df["Price"]

model  = LinearRegression()
model.fit(x,y)

new_house = pd.DataFrame({"Area" : [1600], "Bedroom" : [3],"Bathroom" : [2]})
predict = model.predict(new_house)

print(f"Predicition is {predict[0]}")


